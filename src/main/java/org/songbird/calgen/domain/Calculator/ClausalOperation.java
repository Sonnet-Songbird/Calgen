package org.songbird.calgen.domain.Calculator;

public class ClausalOperation extends Operation {
        public ClausalOperation(Operator operator, Operand operand) {
        super(operator, operand);
    }
}
